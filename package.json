{
  "name": "shop",
  "version": "1.0.0",
  "dependencies": {
    "mustache": "^3.1.0"
  },
  "watch": {
    "build": "./*.mustache"
  },
  "scripts": {
    "build": "mustache data/shop.json index.mustache > index.html",
    "watch": "npm-watch build"
  }
}
